spring:
    datasource:
        url: jdbc:mysql://localhost:3306/midnightoil
        username: root
        password: Hisoka28
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        database-platform: org.hibernate.dialect.MySQL8InnoDBDialect
        generate-ddl: true
        show-sql: true
        hibernate:
            ddl-auto: create-drop
        properties:
            hibernate:
                globally_quoted_indentifiers: true
                dialect: org.hibernate.dialect.MySQL8Dialect
                format_sql: true
        database: mysql
    security:
        user:
            name: user
            password: password
logging:
    level:
      org.springframework.security.web: DEBUG
server:
    port: 8081